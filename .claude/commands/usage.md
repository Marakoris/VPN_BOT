---
description: Quick usage check - tokens and requests
---

# 🔍 Quick Usage Check

## 📊 Встроенная команда (точная статистика)

```
/cost
```

## 📈 Локальное отслеживание (приблизительное)

### Текущая сессия

!`if [ -f /root/claude-docs/.usage/current-session.txt ]; then COUNT=$(cat /root/claude-docs/.usage/current-session.txt); ESTIMATED=$((COUNT * 2000)); echo "Запросов в сессии: $COUNT"; echo "Примерно токенов: ~$ESTIMATED"; echo ""; if [ $COUNT -gt 40 ]; then echo "⚠️  Близко к лимиту! Рекомендуется проверить /cost"; fi; else echo "Отслеживание не начато"; fi`

### Сегодняшняя активность

!`TODAY=$(date +%Y-%m-%d); if [ -f /root/claude-docs/.usage/$TODAY-usage.log ]; then TOTAL=$(wc -l < /root/claude-docs/.usage/$TODAY-usage.log); echo "Всего запросов сегодня: $TOTAL"; echo "Последние 5:"; tail -5 /root/claude-docs/.usage/$TODAY-usage.log; else echo "Сегодня активности не было"; fi`

## ⚠️ Индикаторы проблем

**Симптомы близости к лимиту:**
- 🐌 Медленные ответы (>10 секунд)
- ❌ Ошибки 429 (Rate limit exceeded)
- ⏸️ Необычные паузы между запросами

**Действия:**
1. Запустить `/cost` для точной проверки
2. Подождать 60 секунд (лимиты сбрасываются каждую минуту)
3. Открыть консоль: https://console.anthropic.com/settings/usage

## 💡 Быстрая справка

**Rate Limits (Tier 1, Claude Sonnet 4.5):**
- ⚡ 50 запросов/мин
- 📥 30,000 input токенов/мин
- 📤 8,000 output токенов/мин

**Оптимизация:**
- Используйте Grep вместо Read больших файлов
- Batch операции вместо множественных вызовов
- Избегайте повторного чтения одного и того же

---

**Детальная информация:** `/limits`
