---
description: Check API usage, token limits, and costs
---

# 💰 API Usage & Limits

## 📊 Текущая сессия (встроенная команда)

Используйте встроенную команду Claude Code для точной статистики:

```
/cost
```

Показывает:
- Общая стоимость сессии
- Использованные токены (input/output)
- Время работы API
- Количество изменений кода

## 🔍 Детальная информация

### Rate Limits (в минуту)

Ваши лимиты зависят от tier подписки. Типичные лимиты для Tier 1 (Claude Sonnet 4.5):

| Лимит | Значение |
|-------|----------|
| **Requests** | 50 запросов/мин |
| **Input tokens** | 30,000 токенов/мин |
| **Output tokens** | 8,000 токенов/мин |

### Проверка лимитов онлайн

Откройте Claude Console для детальной статистики:
```
https://console.anthropic.com/settings/usage
```

Там вы увидите:
- ✅ Почасовое использование токенов
- ✅ Текущие rate limits для вашего tier
- ✅ Cache hit rates
- ✅ Графики использования

## ⚠️ Предупреждения о лимитах

### Когда вы близки к лимиту:

**Симптомы:**
- Медленные ответы
- Ошибки 429 (Rate limit exceeded)
- Сообщения о превышении лимита

**Решения:**
1. **Подождать 1 минуту** - лимиты сбрасываются каждую минуту
2. **Использовать /cost** - проверить текущее использование
3. **Оптимизировать запросы:**
   - Читать меньше файлов за раз
   - Использовать более точные grep/glob запросы
   - Избегать повторного чтения одних и тех же файлов

## 💡 Советы по оптимизации

### Экономия токенов:

1. **Используйте точные инструменты:**
   ```
   ❌ Read весь большой файл
   ✅ Grep конкретный паттерн
   ```

2. **Кэширование:**
   - Claude Code автоматически кэширует контекст
   - Повторное чтение = меньше токенов

3. **Batch операции:**
   ```
   ❌ 10 отдельных Edit вызовов
   ✅ 1 Edit с несколькими изменениями
   ```

4. **Используйте model: haiku для простых задач:**
   ```json
   {
     "model": "haiku"
   }
   ```
   Haiku дешевле и быстрее для простых операций.

## 📈 Мониторинг (для продвинутых)

Если хотите детальный мониторинг, настройте OpenTelemetry:

```bash
export CLAUDE_CODE_ENABLE_TELEMETRY=1
export OTEL_METRICS_EXPORTER=prometheus
export OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

claude
```

Затем просматривайте метрики в Prometheus/Grafana.

## 🚨 Быстрая проверка

**Перед важной операцией:**
1. Запустите `/cost`
2. Посмотрите сколько токенов уже использовано
3. Если близко к лимиту - подождите минуту

**Примерная оценка:**
- Чтение большого файла (1000 строк) ≈ 3,000-5,000 токенов
- Простой ответ ≈ 500-1,000 токенов
- Сложная генерация кода ≈ 2,000-5,000 токенов

---

**Быстрые команды:**
- `/cost` - Показать использование в сессии
- Онлайн: https://console.anthropic.com/settings/usage
