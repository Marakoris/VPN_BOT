# 🚀 БЫСТРАЯ ШПАРГАЛКА - VPNHubBot

## ⚡ Быстрое подключение

### ПРОДАКШН (193.124.182.161):
```bash
bash /root/ssh-prod.sh
```

### ТЕСТОВЫЙ (185.58.204.196):
```bash
# Уже на этом сервере, используйте:
cd /root/production_server/VPNHubBot
```

---

## 🎯 ГДЕ ЧТО НАХОДИТСЯ?

| Что | Продакшн | Тестовый |
|-----|----------|----------|
| **IP** | 193.124.182.161 | 185.58.204.196 |
| **Hostname** | vpn.fin-bot | vpn.fin-1 |
| **Папка бота** | `/root/VPNHubBot/` | `/root/github_repos/VPN_BOT/` ✅ |
| **Git репо** | kk2eley/VPNHubBot | Marakoris/VPN_BOT |
| **База данных** | 3000+ реальных юзеров | 9 тестовых юзеров ✅ |
| **Токен бота** | 7433064342:AAH... | 7501968261:AAF... ✅ |
| **Статус** | 🟢 РАБОТАЕТ | 🔴 ОСТАНОВЛЕН |

⚠️ **СТАРЫЙ БЭКАП (НЕ ТРОГАТЬ!):** `/root/production_server/VPNHubBot_BACKUP_PROD_DO_NOT_USE/`

---

## ⚠️ ЗОЛОТОЕ ПРАВИЛО

```
┌─────────────────────────────────────────────┐
│  ПЕРЕД ЛЮБЫМ ДЕЙСТВИЕМ ПРОВЕРЬ:             │
│  hostname  →  Должен быть vpn.fin-bot       │
│  pwd       →  Должен быть /root/VPNHubBot   │
│                                             │
│  ЕСЛИ НЕТ - ТЫ НЕ НА ПРОДАКШНЕ!            │
└─────────────────────────────────────────────┘
```

---

## 📋 Частые команды

### Проверить где я:
```bash
hostname && pwd && docker ps --format "table {{.Names}}\t{{.Status}}"
```

### Посмотреть логи продакшн бота:
```bash
ssh root@193.124.182.161 "docker logs vpnhubbot-vpn_hub_bot-1 --tail 100"
```

### Перезапустить продакшн бот:
```bash
ssh root@193.124.182.161 "cd /root/VPNHubBot && docker compose restart vpn_hub_bot"
```

### Бэкап БД:
```bash
ssh root@193.124.182.161 "docker exec postgres_db_container pg_dump -U marakoris VPNHubBotDB > /app/backups/backup_\$(date +%Y%m%d_%H%M%S).sql"
```

---

## 🔴 ЧТО НЕЛЬЗЯ ДЕЛАТЬ

❌ Запускать тестовый бот одновременно с продакшн (конфликт токенов!)
❌ Тестировать новый код на продакшне
❌ Изменять код на продакшне без тестирования
❌ Работать с продакшн БД напрямую без бэкапа
❌ Перезапускать продакшн без причины

---

## ✅ ПРАВИЛЬНЫЙ ПОРЯДОК ДЕЙСТВИЙ

1. **Разработка:** `/root/github_repos/VPN_BOT/`
2. **Тестирование:** На 185.58.204.196 (с остановленным ботом!)
3. **Commit:** Push в `Marakoris/VPN_BOT`
4. **Бэкап:** Продакшн БД
5. **Деплой:** Pull на продакшн и перезапуск
6. **Мониторинг:** Логи и метрики

---

**Полная документация:** `/root/SERVERS.md`
